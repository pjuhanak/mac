# install
brew install git

# clone public repo to local
git clone https://github.com/pjuhanak/mac

# commit multiple changes at once
git add -A && git commit -m "a change, life is too short"

# sync changes to public repo
git push origin master
